import styles from "./NavBar.module.css";

const NavBar = () => {
    return (
        <div>
            <nav className={styles.navContainer}>
                Logo
            </nav>
        </div>
    )
}

export default NavBar;