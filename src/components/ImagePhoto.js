import styles from "./ImagePhoto.module.css"

const ImagePhoto = () => {
    return (
        <div className={styles.info}>
            <div className={styles.image}/>
        </div>
    )
};

export default ImagePhoto;